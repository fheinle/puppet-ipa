---
version: 5
defaults:
    datadir: data
    data_hash: yaml_data

hierarchy:
    - name: Per node data
      path: 'nodes/%{trusted.cername}.yaml'
    - name: OS specifics
      path: 'os/%{facts.os.family}.yaml'
    - name: Common
      path: 'common.yaml'

